this is my personal project
if you launch in local
you have to launch many process 


1. run docker-compose up -d in a31.ddns.net

2. run nginx in local desktop command is ./nginx

3. start share_media_server

4. run react

5. my visual studio code location is "D:\VSCode-win32-x64-1.107.1\Code.exe"

6. open share_media import project and command "npm install" and command "npm run dev"

7. cd /app/server/dev and do docker-compose up -d  to start kafka

run a31.ddns.net docker-compose up -d
----
cd nginx | start nginx | tasklist /fi "imagename eq nginx.exe"
.\nginx.exe -s quit
----
login
id : anonymous
pwd : anonymous